import { HideForFarmerDirective } from './hide-for-farmer.directive';

describe('HideForFarmerDirective', () => {
  it('should create an instance', () => {
    const directive = new HideForFarmerDirective();
    expect(directive).toBeTruthy();
  });
});
